// eslint-disable-next-line import/no-named-as-default
import styled from 'styled-components';

const ChannelProgramsStyled = styled.div`
    position: relative;
`;

export default ChannelProgramsStyled;
